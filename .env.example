# Copy to .env and fill in real values before running docker-compose.hub.yml.
# .env is gitignored — never commit real secrets here.
#
# Generate strong values with, e.g.:
#   openssl rand -base64 32

MYSQL_ROOT_PASSWORD=
JWT_SECRET=
SENSOR_API_KEY=
IMAGE_VERSION=latest
