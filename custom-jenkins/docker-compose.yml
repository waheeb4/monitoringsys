services:
  jenkins:
    build:
      context: .
    container_name: monitoring-jenkins
    user: "0:0"
    ports:
      - "8088:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  jenkins_home:
